<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <title>JsonServer</title>
  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
</head>
<body>
  <% if flash[:error].present? %>
    <div class="alert alert-warning text-center" role="alert"><%= flash[:error] %></div>
  <% end %>

  <% if flash[:success].present? %>
    <div class="alert alert-success text-center" role="alert"><%= flash[:success] %></div>
  <% end %>    

  <div class="pull-right">
    <span><%= @current_user.nickname %></span>
    <a href="/sessions/destroy"><%= t('logout') %></a>
  </div>

  <%= yield %>

  <div class="footer">
    <div class="language">
      <% if I18n.locale == I18n.default_locale %>
        <%= link_to "简体中文", '/profiles/change_locale?locale=zh-CN' %>
        <span class="english">English</span>
      <% else %>
        <span class="chinese">简体中文</span>
        <%= link_to "English", '/profiles/change_locale?locale=en' %>
      <% end %>
    </div>
  </div>

</body>
</html>